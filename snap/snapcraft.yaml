
name: snap-dash # you probably want to 'snapcraft register <name>'
version: '0.0.1'
summary: Dash Coin node as a Snap package
description: |
  Dash Coin node as a Snap package

grade: devel
confinement: devmode # use 'strict' once you have the right plugs and slots

apps:
  dash:
    command: bin/dashd
    daemon: simple

parts:
  dash:
    plugin: autotools
    source: https://github.com/dashpay/dash/archive/v0.12.1.5.tar.gz
    configflags:
     - "--disable-wallet"
     - "--disable-tests"
     - "--disable-gui-tests"
     - "--disable-bench"
     - "--disable-ccache"
     - "--disable-zmq"
     - "--with-daemon"
    build-packages:
     - build-essential
     - libtool
     - autotools-dev
     - automake
     - pkg-config
     - libssl-dev
     - libevent-dev
     - bsdmainutils
     - libdb-dev
     - libdb++-dev
     - libboost-system-dev
     - libboost-filesystem-dev
     - libboost-chrono-dev
     - libboost-program-options-dev
     - libboost-test-dev
     - libboost-thread-dev
